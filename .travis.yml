language: go

matrix:
  include:
    - os: linux
      dist: bionic

go:
  - 1.13.4

env:  GO111MODULE=on

git:
  depth: 1

notifications:
  email: false

script:
  - cd $TRAVIS_BUILD_DIR
  - ./build.sh
