\* KvsSectorFalseLeftover TLC 設定 (Phase FL1 バグ再現 / FL3 誤検知 safety)
\* safety のみ。実行フェーズは KvsSectorFalseLeftoverMC.tla のコメントを参照

SPECIFICATION Spec

CONSTANT Nodes <- MC_Nodes
CONSTANT InitialMembers <- MC_InitialMembers
CONSTANT MaxChurn <- MC_MaxChurn
CONSTANT MaxStuck <- MC_MaxStuck
CONSTANT MaxViewDrops <- MC_MaxViewDrops
CONSTANT EnableRelease <- MC_EnableRelease
CONSTANT PermissiveRelease <- MC_PermissiveRelease
CONSTANT ClipActivationTail <- MC_ClipActivationTail
CONSTANT FixConfirmVictim <- MC_FixConfirmVictim
CONSTANT FixScopedTerminate <- MC_FixScopedTerminate
CONSTANT FixNoActivateUnderCover <- MC_FixNoActivateUnderCover
CONSTANT FixNoAbsorbWhileLocked <- MC_FixNoAbsorbWhileLocked

CHECK_DEADLOCK FALSE

\* ── Safety のみ ──
INVARIANT TypeOK
INVARIANT NoRepairLoss
INVARIANT ValidRange
INVARIANT ActiveFlagConsistent
INVARIANT LockOnExistingSector
INVARIANT HolderOnActiveSector
INVARIANT FlagsOnActiveSector
